package ai.controlCenter;

import model.game.CIClient;
import ai.helper.AIHelper;
import ai.helper.AiModelAnalyzer;

/**
 * Exploration decides whether an unit must explore the map
 *
 */
public class Exploration {
	
	private CIClient ciClient = null;
	private AIHelper aiHelper = null;
	private AiModelAnalyzer aiModel = null;
	private String myUserAssetsID = null;
	
	public Exploration(CIClient ciClient, AIHelper  aiHelper, AiModelAnalyzer aiModel) {
		super();
		this.ciClient = ciClient;
		this.aiHelper = aiHelper;
		this.aiModel = aiModel;
		
		this.myUserAssetsID = this.aiModel.getMyUserAssetsCounter().getId();
		
	}
	
	/**
	 * this method decide whether units must explore the map
	 * 
	 * @param mainAi
	 * 		--> main ai class
	 */
	public void decideExploration(AIMain mainAi) {
		// TODO decision whether any units must explored
		
	}
	
}
